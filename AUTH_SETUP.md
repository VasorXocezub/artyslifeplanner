I added api/check-user.js.

Next:
1. Replace src/Auth.jsx with the version we discussed (or ask me to generate it from the extracted project).
2. Add VITE_SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY in Vercel.
3. Deploy.
